# Agent Specs

Use this directory for evolving the `.agents` system itself.

Examples:
- orchestrator changes
- workflow changes
- new specialist roles
- changes to hooks, prompts, or governance

Keep service-feature specs under the sample's `changelogs/changes/` tree unless the change is primarily about the agent operating model.
